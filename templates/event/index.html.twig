
<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>my Rem events</title>


    <!-- Bootstrap core CSS -->
    {{ encore_entry_link_tags('app')}}

    <style>
        .bd-placeholder-img {
            font-size: 1.125rem;
            text-anchor: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        @media (min-width: 768px) {
            .bd-placeholder-img-lg {
                font-size: 3.5rem;
            }
        }
    </style>
    <!-- Custom styles for this template -->
    <link href="https://fonts.googleapis.com/css?family=Playfair+Display:700,900" rel="stylesheet">
    <!-- Custom styles for this template -->
    <link href="blog.css" rel="stylesheet">

</head>
<body>
<div class="container">
    <header class="blog-header py-3">
        <div class="row flex-nowrap justify-content-between align-items-center">
            <div class="col-4 pt-1">
                <a class="text-muted" ></a>
            </div>
            <div class="col-4 text-center">
                <a class="blog-header-logo text-dark" >JE CREE OU MODIFIE OU SUPPRIME UN EVENEMENT </a>
            </div>
            <div class="col-4 d-flex justify-content-end align-items-center">
                <a class="text-muted" href="/">RETOUR</a>
            </div>
        </div>
    </header>



    <!-- body-->
    <div>
        {% for message in app.flashes('notice_success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
    </div>
    <div>
        {% for message in app.flashes('notice_error') %}
            <div class="alert alert-danger">
                {{ message }}
            </div>
        {% endfor %}
    </div>
    <br/>
    <br/>
    <br/>
    <br/>


        <div class="col-md-12">
                <div align="center">
                    <p>Merci de remplir les champs ci - dessous pour créer un évènement </p>
                </div>
                <br/>
                <br/>
                <div class="col-md-12">
                    <div class="col-md-9" style="max-width: 100%;">
                        {{ form_start(EvenementForm) }}
                        {{ form_row(EvenementForm.nom) }}
                        {{ form_row(EvenementForm.datedebut) }}
                        {{ form_row(EvenementForm.datefin) }}
                        {{ form_row(EvenementForm.capaciteaccueil) }}
                        <br/>
                        {{ form_row(EvenementForm.creer) }}
                        {{ form_end(EvenementForm) }}
                    </div>
                </div>
        </div>
    <br/>
    <br/>
    <div class="row flex-nowrap justify-content-between align-items-center">
        <div class="col-4 pt-1">
            <a class="text-muted" ></a>
        </div>
        <div class="col-4 text-center">
            <a class="blog-header-logo text-dark" >MODIFICATION OU SUPPRESSION D'UN EVENEMENT </a>
        </div>
        <div class="col-4 d-flex justify-content-end align-items-center">
            <a class="text-muted" ></a>
        </div>
    </div>
        <div class="col-md-12">
            <div align="center">

            </div>
            <br/>

            <br/>
            <div class="col-md-12">
                <div class="col-md-9" style="max-width: 100%;">
                    {% for event in event %}
                        <div class="container-fluid">

                            <ul class="list-inline" style="border: 1px solid;border-radius: 3px;padding: 10px;position: relative;">
                                <li class="list-inline-item" style="margin-left: 25px;margin-right: 25px;">{{ event.nom }}</li>
                                <li class="list-inline-item" style="margin-left: 25px;margin-right: 25px;">{{ event.datedebut |date('d-m-Y') }}</li>
                                <li class="list-inline-item" style="margin-left: 25px;margin-right: 25px;">{{ event.datefin |date('d-m-Y') }}</li>
                                <li class="list-inline-item" style="margin-left: 25px;margin-right: 25px;">{{ event.status }}</li>
                                <li class="list-inline-item" style="margin-left: 25px;margin-right: 25px;color:red; float: right"><a href="#" style="color:red;">Supprimer</a></li>
                                <li class="list-inline-item" style="margin-left: 25px;margin-right: 25px;float: right"><a href="#">Modifier</a></li>

                            </ul>

                        </div>
                    {% endfor %}
                </div>
            </div>


<script>
    jQuery(document).ready(function() {
        $('.js-datepicker').datepicker();
    });
</script>
{{ encore_entry_script_tags('app')}}


</body>
</html>


